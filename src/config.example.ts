// Импортируем типизацию
import { Config } from "./typings";

let config: Config = {
  token: "", // Здесь вводите ваш токен
};
// Экспортируем конфиг
export default config;

// Если название файла config.example.ts то поменяйте название на config.ts